name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-release:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      
      - name: Select Xcode 12
        uses: devbotsxyz/xcode-select@v1
        with:
          version: "12"
      
      - name: Build binary
        run: xcodebuild -configuration Release

      - name: Package to zip
        run: zip -rj ComboJack-Release-${GITHUB_REF##*/}.zip scripts/* ComboJack-Swift/build/Release/ComboJack-Swift

      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          files: ComboJack-Release-${GITHUB_REF##*/}.zip
          draft: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}